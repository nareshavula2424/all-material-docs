
Lambda Expression:
==================
=> Lambda Expression is a concise way to represents the anonymus function. it allows us to write functional style of code
   that is more readable and expressive.

   🔹 Syntax of Lambda Expressions:
   =================================
   () -> {}

    () -  input arguments
    -> - Arrow, it seperates input arguments and body
    {} - body(it contains logic, single line or multi line)

    
